paste it in your workbench in mysql

create database hackton;
create table optionsheet (question_id varchar(1000), option1 varchar(1000),
option2 varchar(1000),option3 varchar(1000), option4 varchar(1000),question_no autoincrement);

create table test (question varchar(1000),answer varchar(1000));

create table user_info(user_name varchar(1000),password varchar(1000));

create table online_test(Username_test varchar(1000),score int); // score wala the table

//insertion  of the databases want to make it yourself ;

//update purpose sql query;
UPDATE `online_test` SET `userid` = '1' WHERE `online_test`.`score` = 0;

create table exam_record(user_name varchar(1000), total_score int);

create table teacher_info(Username varchar(1000),Password varchar(1000),Institute_name varchar(1000) );
online_test database only working during the online_test going on after it delete the student record;
